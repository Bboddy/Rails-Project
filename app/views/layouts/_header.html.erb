<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <i class="fas fa-subway"></i>
      Rails Store
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <%= link_to root_path, class: "nav-link #{'active fw' if current_page?(root_path)}" do %>
          <div class="fa fa-home"></div>
          <%= link_to "Home", root_path%>
        <% end %>
        <%= link_to root_path, class: "nav-link #{'active fw' if current_page?(root_path)}" do %>
          <div class="fas fa-store"></div>
          <%= link_to "Store", '/categories'%>
        <% end %>
        <%= link_to root_path, class: "nav-link #{'active fw' if current_page?(root_path)}" do %>
          <div class="fas fa-shopping-cart"></div>
          <%= link_to "Cart", cart_path(current_user.id)%>
        <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <%= link_to destroy_user_session_path, method: :delete, class: "nav-link #{'active fw' if current_page?(root_path)}" do %>
            <div class="fa fa-sign-out-alt"></div>
            <b>Sign out</b>
          <% end %>
          <%= link_to edit_user_registration_path, class: "nav-link #{'active fw' if current_page?(root_path)}" do %>
            <div class="fa fa-user"></div>
            <b><%= current_user.email %></b>
          <% end %>
        <% else %>
          <%= link_to "Log in", new_user_session_path, class: "nav-link #{'active fw' if current_page?(new_user_session_path)}" %>
          <%= link_to "Sign up", new_user_registration_path, class: "nav-link #{'active fw' if current_page?(new_user_registration_path)}" %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>